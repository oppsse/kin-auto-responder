import React from 'react';
import ReactDOM from 'react-dom/client';
import KinAutoResponder from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <KinAutoResponder />
  </React.StrictMode>
);
